sandsmark-xdg-autostart-launcher
================================

Minimalistic (315 lines of code, no dependencies) autostart launcher following
the XDG specification.


Dependencies
------------

 - A (sane) C++ compiler
 - CMake (for building)


Usage
-----

`xdg-autostart-launcher (--system|--user|--both) [--verbose]`
